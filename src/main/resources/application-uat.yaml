logging:
  level:
    root: WARN
    "[com.example.connector]": DEBUG

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/connector?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: ${MYSQL_ROOT_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
