package com.example.connector.repo.gw;

import com.example.connector.entity.gw.GwDeviceInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GwDeviceInfoRepository extends JpaRepository<GwDeviceInfo, Long> {}
