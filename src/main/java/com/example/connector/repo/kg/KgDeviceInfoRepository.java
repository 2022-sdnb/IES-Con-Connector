package com.example.connector.repo.kg;

import com.example.connector.entity.kg.KgDeviceInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KgDeviceInfoRepository extends JpaRepository<KgDeviceInfo, Long> {}
