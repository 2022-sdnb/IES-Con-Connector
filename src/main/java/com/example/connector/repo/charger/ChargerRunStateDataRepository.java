package com.example.connector.repo.charger;

import com.example.connector.entity.charger.ChargerRunStateData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChargerRunStateDataRepository extends JpaRepository<ChargerRunStateData, Long> {}
