package com.example.connector.repo.charger;

import com.example.connector.entity.charger.ChargerRunPowerData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChargerRunPowerDataRepository extends JpaRepository<ChargerRunPowerData, Long> {}
